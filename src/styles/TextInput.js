import { scale, verticalScale } from "react-native-size-matters";


export const TextInputLargeRounded = {
      textAlign: "center",
      borderRadius: 20,
      width: scale(300),
      height: verticalScale(40),
      marginVertical: verticalScale(10),
}
